<?xml version="1.0" encoding="UTF-8"?>
<svg width="<%= size %>" height="<%= size %>" viewBox="0 0 <%= size %> <%= size %>" style="font-family: Helvetica Neue, Helvetica, PingFang SC, Hiragino Sans GB, Microsoft YaHei, SimSun, sans-serif" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
  <rect width="<%= size %>" height="<%= size %>" rx="15%" ry="15%" fill="#<%= color %>"></rect>
  
  <% if (checked) { %>
  <polyline 
    points="<%= points %>"
    fill="transparent"
    stroke="#FFF"
    stroke-width="<%= 50 / 300 * size %>"
  />
  <% } %>
</svg>